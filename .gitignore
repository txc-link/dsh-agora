# Private docs (OpenClaw copies)
/docs/private/
/docs/
*.db
*.db-wal
*.db-shm
__pycache__/
*.pyc
.venv/
dist/
dashboard/.artifacts/
.agora-ts/
.env
.env.local
.env.*.local
*.egg-info/

# TypeScript build cache
*.tsbuildinfo
/.worktrees/
/worktrees/

# Process notes must live under docs/plans/<task-folder>/, never at repo root
/task_plan.md
/findings.md
/progress.md
/notes.md
agora-ts/apps/server/archive-outbox

# nested clone of dsh-matrix-connector used by dsh-agora for R-B development
.repos/
smoke-data
.secrets/

# Sandbox / local-only state (2026-08-31 next-batch hygiene)
# .home/ holds the user's runtime state mirror (.agora, .npm, .skills)
# .npm-cache* dirs are sandbox-redirected npm scratch dirs; they must
# never enter git (per AGENTS.md §8 — no bulk state).
/.home/
/.npm-cache/
/.npm-cache-install/
