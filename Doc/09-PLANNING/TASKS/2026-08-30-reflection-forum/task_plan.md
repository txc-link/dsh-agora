# Task Plan: Reflection + Forum (S6)

> 蓝图 05 · worktree .dsh/workspaces/reflect (feat/reflection-forum)

未决默认拍板: 存储=SQLite org_forum_posts/comments (039, SSoT 单一; obsidian 映射留 S4); 频率=手动 CLI 触发; 进化=建议+显式应用(不静默改配置, §2 审批精神); 可见性=项目作用域

R1: contracts ForumPost/Comment + IForumRepository + migration 039 + repo
R2: ForumService (CRUD+search+relevantPosts 学习检索) + ReflectionService (scorecard→确定性反思报告)
R3: CLI agora post/forum/reflect + 回归 + 冒烟 + merge + 回写
