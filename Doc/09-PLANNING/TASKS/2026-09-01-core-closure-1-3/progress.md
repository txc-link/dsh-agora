# Progress

## 2026-09-01

- [x] 建立独立 worktree/feature branch。
- [x] 新增 `GovernedDispatchEnvelope` contract 与 `GovernedDispatchService`。
- [x] approved/active collaboration plan task 启用 runtime strict governance；CoordinationService 自动转换 envelope。
- [x] 新增 task timeline contract/service、runtime dispatch 按 task 查询、REST timeline endpoint。
- [x] 新增 stale claim 自动过期、Core takeover、CLI takeover、REST takeover endpoint。
- [x] Markdown artifact parent/version/diff metadata、versions/task artifacts/review endpoints。
- [x] 聚焦测试 43/43（含 coordination/company routes）；workspace build 通过；core architecture/barrel gates 通过；改动文件 lint 通过。
- [ ] 完成本分支提交、fast-forward merge、推送远端。
