# Progress — Element agent workspace sample

## 2026-08-30

- Read repository rules and confirmed a clean `master` worktree.
- Read organization, shared-memory, personal-office, governance, voice, Matrix
  connector, Mem0 adapter, Obsidian adapter, citizen CLI, company CLI, and
  relationship contracts.
- Checked official Element documentation for private Spaces/rooms, files,
  threads, permissions, history visibility, and encryption behavior.
- Added a copyable walkthrough plus conceptual agent cards and an executable
  companion relationship payload.
- Verification completed:
  - companion JSON parsed with PowerShell `ConvertFrom-Json`;
  - agent-card YAML parsed with PyYAML;
  - all relative Markdown links resolve;
  - sample files contain none of the known live credential prefixes;
  - `git diff --check` passes (the existing Windows LF/CRLF warning is
    informational).
