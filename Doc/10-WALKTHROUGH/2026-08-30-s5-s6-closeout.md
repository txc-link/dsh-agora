# S5/S6 收尾 — checklist 清零

- 日期: 2026-08-30 | commit: `c0e5c9a` | planning: Doc/09-PLANNING/TASKS/2026-08-30-s5-s6-closeout/

## 交付

### S5-61 调研结果自动写回共享记忆
- `AgentQuestionService` 新增可选 `groupMemory` 缝（core 零平台名, mem0 由 composition 注入）
- `answer()` 中 kind==='research' → `add({scopeRef, kind:'research', text: question+answer})`
- 语义: **await + try/catch**——初版 fire-and-forget 在 CLI 进程退出时把 in-flight POST 砍掉（冒烟暴露）, 改为等待完成但失败不阻塞 answer
- CLI: `getAgentQuestionService` 注入 `{ add: (i) => getGroupMemoryService().record(i) }`

### S6-67 AgentEvolutionService（建议+确认）
- 设计拍板: 反思报告**不隐式改配置文件**（core 不写死配置格式）; 建议落 forum proposal 帖, 人/agent 显式 apply
- 状态机: `metadata.evolution.status: proposed → applied`（幂等拒绝重复 apply; 非 proposal 帖拒）
- 支撑改动: `IForumRepository.updatePost`（contracts+db, 最小可变能力仅 metadata patch）+ `ForumService.updatePostMetadata` + `CreatePostInput.metadata` 透传
- CLI: `agora evolution propose --agent --project`（reflect→proposal 帖一步）+ `agora evolution apply --post --by`

## 验证

- 全量 1423/1423 + 双 gate ✅
- 冒烟 1（真 CLI+真 mem0）: ask research → answer → mem0 `task:org` scope 下两条 research 条目（含 answer 正文）✅
- 冒烟 2（真 CLI+真库）: proposal 帖 → `evolution apply` → SQLite metadata.evolution={status:'applied', applied_by:'human:ceo'} ✅

## checklist 终态

S1–S6 全部可实现条目 ✅; 唯余 federation P2（跨节点 L4 记忆）/ P3（自动团队组建）两项**明确留待**——依赖多 homeserver 环境, 由用户启动, 不做超前开发。
