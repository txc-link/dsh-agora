# Undecided

1. 陪伴者独立 Matrix 用户采用普通 bot 账号还是 Application Service 虚拟用户。
2. 首个正式声音使用本机 SAPI、云端 TTS，还是后续本地神经 TTS。
3. SharedEpisode 的用户确认粒度与自动过期规则。
4. 主动关心生成器复用 citizen runtime 还是建立专用 resident runtime lease。
5. 伴侣短期情绪状态是否持久化；当前倾向只保存可衰减状态，不作为事实记忆。
6. E2EE 密钥的备份与 break-glass 恢复主体。
7. 是否允许以后接收用户语音并做 STT；若允许，原始音频保留多久。

