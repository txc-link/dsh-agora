# Undecided

1. Whether a future conflict UI may choose `done` or `cancelled` and explicitly repair the losing provider.
2. Whether Google push channels should replace polling after enough bound events exist to justify webhook lifecycle complexity.
3. Whether mutable fields should later use a three-way merge with per-field ownership instead of remaining one-way/manual.
4. Whether TickTick/滴答 offers a documented reopen endpoint for the user's account region in a future API version.
